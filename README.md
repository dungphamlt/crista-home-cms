# Crista Home - CMS Admin

Trang quản trị Crista Home (Next.js).

## Chạy

```bash
npm install
npm run dev
```

Cấu hình `.env` với `NEXT_PUBLIC_API_URL` trỏ tới API backend.

## Biến môi trường

- `NEXT_PUBLIC_API_URL` - URL API backend (VD: http://localhost:3002)
